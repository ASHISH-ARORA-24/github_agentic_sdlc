[project]
name = "inventory-service"
version = "0.1.0"
description = "Inventory service — stock management for the ecommerce sample"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

# src/hitl/ — Human-in-the-Loop
#
# This package contains all points where the pipeline pauses and waits for human input.
# These are NOT tools (the LLM does not call them).
# The orchestrator calls them directly and blocks until the human responds.
